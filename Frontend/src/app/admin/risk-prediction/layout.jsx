export const metadata = {
  title: "Risk prediction | Admin | DMEWS",
  description: "District risk forecasts and Power BI API endpoints.",
};

export default function AdminRiskPredictionLayout({ children }) {
  return children;
}
