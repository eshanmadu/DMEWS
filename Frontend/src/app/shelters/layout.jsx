export const metadata = {
  title: "Shelters | DMEWS",
  description: "Find evacuation shelters. When logged in, see shelters in your district first. Safety do's and don'ts.",
};

export default function SheltersLayout({ children }) {
  return children;
}
