export const metadata = {
  title: "Risk prediction | DMEWS",
  description: "District risk forecasts, charts, and model confidence.",
};

export default function RiskPredictionLayout({ children }) {
  return children;
}
